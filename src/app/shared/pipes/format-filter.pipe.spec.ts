import { FormatFilterPipe } from './format-filter.pipe';

describe('FormatFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
